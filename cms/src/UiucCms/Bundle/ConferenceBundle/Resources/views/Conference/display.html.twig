{% extends 'UiucCmsConferenceBundle::layout_nav.html.twig' %}

{% block content %}
<div class="container">
    <div class="page-header" style="text-align:left">
        <h1>
            Details for {{ conference.getName() }}
        </h1>
    </div>
    <p class="lead">
        Year: {{ conference.getYear() }} <br>
        City: {{ conference.getCity() }} <br>
        Registery Begins: {{ conference.getRegisterBeginDate().format('Y-m-d:i:s') }} <br>
        Registery Ends: {{ conference.getRegisterEndDate().format('Y-m-d:i:s') }} <br>
    </p>
    <p>
        {% if enrolled == false %}
            <li><a href="{{ path('uiuc_cms_conference_enroll', {'id': conference.getId() }) }}">Enroll in this conference</a>
        {% else %}
            You have already enrolled in this conference.
        {% endif %}
        
        <li><a href="{{ path('uiuc_cms_conference_homepage') }}">Back to listings</li>
    </p>
</div>
{% endblock content %}
